// ✍️  Módulo de Strings - utils/strings.js

// TODO: Exporta la función capitalize
// Debe convertir la primera letra en mayúscula
// Ejemplo: capitalize('hello') → 'Hello'
/*
export const capitalize = str => {
  if (!str) return '';
  return str.charAt(0).toUpperCase() + str.slice(1);
};
*/

// TODO: Exporta la función truncate
// Debe limitar la longitud del string y agregar '...' si es necesario
// Ejemplo: truncate('Hello World', 5) → 'Hello...'
/*
export const truncate = (str, length) => {
  if (str.length <= length) return str;
  return str.slice(0, length) + '...';
};
*/

// TODO: Exporta la función reverse
// Debe invertir el string
// Ejemplo: reverse('abc') → 'cba'
/*
export const reverse = str => {
  return str.split('').reverse().join('');
};
*/
