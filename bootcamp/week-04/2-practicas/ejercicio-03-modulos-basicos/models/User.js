// 👤 Modelo User - models/User.js

// TODO: Crea una clase User
/*
class User {
  constructor({ id, name, email }) {
    this.id = id;
    this.name = name;
    this.email = email;
    this.createdAt = new Date();
  }
  
  // Getter para obtener las iniciales del nombre
  get initials() {
    return this.name
      .split(' ')
      .map(word => word[0])
      .join('')
      .toUpperCase();
  }
  
  // Método para obtener información formateada
  getInfo() {
    return `${this.name} (${this.email})`;
  }
}
*/

// TODO: Exporta User como default export
// export default User;
