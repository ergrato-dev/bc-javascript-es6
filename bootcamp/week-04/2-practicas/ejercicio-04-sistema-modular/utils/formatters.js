// 🎨 Formateadores - utils/formatters.js

// TODO: Exporta formatDate
// Debe formatear una fecha a formato español (dd/mm/yyyy)
/*
export const formatDate = date => {
  if (!date) return 'Sin fecha';
  
  const d = new Date(date);
  return d.toLocaleDateString('es-ES', {
    day: '2-digit',
    month: '2-digit',
    year: 'numeric'
  });
};
*/

// TODO: Exporta formatPriority
// Debe convertir priority code a texto en español
/*
export const formatPriority = priority => {
  const priorities = {
    low: 'Baja',
    medium: 'Media',
    high: 'Alta'
  };
  return priorities[priority] || priority;
};
*/
